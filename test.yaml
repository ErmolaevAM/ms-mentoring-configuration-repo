server:
  port: 8888

spring:
  application:
    name: erlx-config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ErmolaevAM/ms-mentoring-configuration-repo
